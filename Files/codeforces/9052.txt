#include<bits/stdc++.h>
using namespace std;

#define ll long long
bool isGood(const string& s) {
    return all_of(s.begin(), s.end(), [&](char c) { return c == s[0]; });
}

void aditya(){
    ll n,m;
    cin>>n>>m;
    string s,t;
    cin>>s>>t;
    if(isGood(s)){
        cout<<"YES";
        return;
    }
    if(isGood(t)==false){
        cout<<"NO";
        return;
    }
    bool flag1=0,flag2=0;
    for(int i=0;i<n-1;i++){
        if(s[i]==s[i+1]){
            if(s[i]==0) flag1=1;
            else flag2=1;
        }
    }
    if(flag2 && flag1){
        cout<<"NO";
        return;
    }
    if(flag2){
       if(t.front()=='0' && t.back()=='1') cout<<"YES";
       else cout<<"NO";
       return;
    }
}
 int main(){
     int t;
     cin>>t;
     while(t--){
         aditya();
         cout<<endl;
     }
 }