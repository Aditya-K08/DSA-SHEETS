#include<bits/stdc++.h>
using namespace std;

void solve(){
    int x, n;
    cin >> x >> n;
    vector<pair<int, int>> p(x);
unordered_map<int,int>map;
    for(int i = 0; i < x; i++){
	 cin >> p[i].first >> p[i].second;
	for(int j=p[i].first;j<p[i].second;j++){
		map[j]++;
	}
}

    int maxi=INT_MIN;
 	int mini =INT_MAX:
	for(auto it:map){
		if(it.second>maxi) maxi=it.second;
		if(it.secodn<mini) mini=it.second;
	}

    cout << maxi - mini << endl;
}

int main(){
    int test_case;
    cin >> test_case;

    while(test_case-- > 0){
        solve();
    }

    return 0; 
}
